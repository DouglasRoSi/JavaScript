function gerar() {
    numero = 8
    n = Number(numero)
    for(c=1; c <= 10; c++){
        res = n * c
        console.log(`${n} x ${c} = ${res}`)
    }

}
 //chamada = gerar()
 console.log(gerar())